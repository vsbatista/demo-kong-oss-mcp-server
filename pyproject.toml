[project]
name = "kong-mcp-server"
version = "0.1.0"
description = "MCP server for Kong Gateway administration"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.28.0,<2",
    "httpx>=0.28.1",
]

[project.scripts]
kong-mcp = "kong_mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
